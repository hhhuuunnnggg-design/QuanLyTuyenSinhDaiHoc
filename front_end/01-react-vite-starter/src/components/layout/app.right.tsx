// app.left.tsx
const AppRight = ({ className }: { className?: string }) => {
  return <div className={className}>this is app left</div>;
};

// Tương tự cho app.center.tsx và app.right.tsx
export default AppRight;
